#!/usr/bin/env bash
echo "-----> Running collectstatic..."
    source bin/activate
    MANAGE_FILE=$(ls **/manage.py | head -1)
	echo "Running collectstatic"
    python $MANAGE_FILE collectstatic --noinput
fi
